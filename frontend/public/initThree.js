// minimal placeholder — voxel renderer (initVoxel.js) is primary visual initializer
(function(){ if(window.initThree) return; window.initThree = function(){ console.info('initThree placeholder — use initVoxel for voxel visuals'); return false; }; })();
